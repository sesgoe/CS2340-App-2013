package edu.gatech.wheresmystuff.views;

/**
 * Interface to the enter person view.
 * 
 * Tell the view to go to next screen
 * Also tell the view if we need an error alert
 * 
 * @author Ses
 *
 */
public interface IEnterPersonView {
	
	/**
	 * Advance to next screen based on screenID
	 * @param screenID
	 */
	void advance(int screenID);
	
	/**
	 * Functionality for errorAlerts
	 * @param message
	 */
	void errorAlert(String message);

}
